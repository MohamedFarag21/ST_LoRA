# ST-LoRA — core Python dependencies (pip). Frozen from the `ssl` conda env
# (Python 3.11.5). Install PyTorch FIRST from the CUDA 12.6 wheel index, then the rest.
#
#   pip install torch==2.7.0 torchvision==0.22.0 --index-url https://download.pytorch.org/whl/cu126
#   pip install -r requirements.txt
#
# A full `pip freeze` of the environment is kept in requirements_ssl_full.txt.
# NOTE: the EoMT experiments use a SEPARATE env with transformers==5.14.1 (see README);
# Mask2Former + SegFormer use transformers==4.52.2 as pinned below.

# --- deep learning core ---
torch==2.7.0
torchvision==0.22.0
pytorch-lightning==2.5.1.post0
lightning==2.5.1.post0

# --- HuggingFace stack (Mask2Former / SegFormer arch) ---
transformers==4.52.2
peft==0.15.2
accelerate==1.7.0
huggingface-hub==0.31.4
safetensors==0.5.3
evaluate==0.4.3

# --- metrics / uncertainty ---
torchmetrics==1.7.1
torch_uncertainty==0.5.0

# --- scientific / imaging ---
numpy==2.2.6
scipy==1.15.3
scikit-learn==1.6.1
scikit-image==0.25.2
pillow==11.2.1

# --- plotting / io / misc ---
matplotlib==3.10.3
tqdm==4.67.1
PyYAML==6.0.2
